#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @c: first argument
 * @d: second argument
 * Return: the value of c + d
 */

int add(int c, int d)
{
	return (c + d);
}
